[project]
name = "edudash-server"
version = "0.1.0"
dependencies = [
  "flask",
  "flask_cors",
  "flask_sqlalchemy",
  "bcrypt",
  "google-books-api-wrapper"
]

extend-exclude = [
  "*.pyc",
  "__pycache__"
]
